/**
 * 패키지 버전 상수.
 *
 * 배럴(`index.ts`)이 아니라 별도 모듈에 두는 이유는 순환 참조 방지다 —
 * `simulation/report.ts` 처럼 배럴이 재export 하는 모듈이 버전을 필요로 할 때,
 * 배럴에서 가져오면 초기화 순환이 생긴다.
 */

/** 패키지 시맨틱 버전. 호환성 깨지는 변경 시에만 메이저를 올린다. */
export const GAME_CORE_VERSION = '1.0.0'
